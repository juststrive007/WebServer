WebServer 处理客户端请求总共分为三大步：
1.解析请求
2.处理请求
3：响应客户端

本版本开始完成第一步：解析请求的工作
通过了解HTTP协议对请求Request（客户端发送给服务端的内容）得知
，一个请求由三部分构成：
1.请求行 2.消息头 3消息正文
并且请求行与消息头都是文本内容，对应字符集：
ISO08859-1 而且特点都是一CR，LF结尾（回车符（13）与换行符(10)）
正文部分是2进制数据，为用户提交内容。

因此，本版本先做一个测试，完成读取客户端发送过来的一行字符
串操作（一CRLF结尾的一行内容）。因为请求行与消息头
都可以用这个方式来读取。