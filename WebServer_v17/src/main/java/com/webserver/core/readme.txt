上一个版本中我们完成了注册和登录两个功能，随之带来一个问题：每次添加新业务，ClientHandler都要加一个分支
，因此，版本将要解决这个问题。

首先，我们对所有业务处理类进行规范化定义：
定义一个统一的超类：HttpServlet,并让所有的ServLet都继承它
在这里我们定义抽象方法service 作为处理具体业务的方法，这样将来ClientHandler就可以
将不同业务处理类统一看成HttpServlet了，从而调用service方法。
注:如果不这样做，不同的业务处理类的处理业务方法可以自行决定的话，那么clinetHandler
就没办法用统一的方法派发任务了。

