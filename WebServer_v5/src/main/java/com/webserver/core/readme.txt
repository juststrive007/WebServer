此版本开始完成第三步，响应客户端的工作
上一个版本中，clientHandler根据请求中的抽象路径去webapps
目录下寻找用户请求的资源，并以分支的形式分别打桩。此版本
先处理找到资源的情况，我们要将用户请求的页面发送给客户端。

注：
服务端要想给客户端发消息，也必须遵循HTTP协议的要求
，发送一个响应（HTTPResponse）内容。
而响应的具体格式参考文档或者http.txt文本
